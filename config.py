from pathlib import Path

# Sandbox directory (will validate later)
SANDBOX_ROOT = Path("/srv/agent-redteam/sandbox-50cef2fa8d")

# Allowed hosts
ALLOWED_HOSTS = {
    "example.com",
    "www.iana.org"
}
